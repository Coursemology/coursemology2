en:
  activerecord:
    errors:
      models:
        course/video/session:
          attributes:
            session_start:
              cannot_be_after_session_end: 'Session cannot start after ending'
