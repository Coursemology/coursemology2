zh:
  activerecord:
    attributes:
      course/assessment/tab/title:
        # default: 'Default'
        default: '默认值'
      course/assessment/tab:
        # weight: 'Order'
        weight: '权重'
    errors:
      models:
        course/assessment/tab:
          # deletion: 'the last tab cannot be deleted'
          deletion: '最后一项无法被删除'
