zh:
  activerecord:
    errors:
      models:
        course/group_user:
          # not_enrolled: 'must be enrolled in same course as group'
          not_enrolled: '必须与小组成员加入在同一课程'
    attributes:
      course/group_user:
        # manager: 'Tutor'
        manager: '讲师'
