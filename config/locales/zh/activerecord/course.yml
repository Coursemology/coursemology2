zh:
  activerecord:
    attributes:
      course:
        reference_timelines: '引用时间线'
    errors:
      models:
        course:
          attributes:
            reference_timelines:
              must_have_at_most_one_default: '至少要包含一个默认值'
