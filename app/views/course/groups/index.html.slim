= page_header do
  = new_button([current_course, :group]) if can?(:create, Course::Group.new(course: current_course))

table.table.table-hover
  thead
    tr
      th.text-center = t('common.serial_number')
      th = t('.name')
      th = t('.members')
      th = t('.phantom')
      th = t('.total')
      th = t('.managers')
      th

  tbody
    = render @groups
