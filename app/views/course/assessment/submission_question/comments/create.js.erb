$('#<%= comments_container_id(@submission_question) %>').html(
  '<%= escape_javascript(render partial: 'course/assessment/submission_question/comments',
                                locals: { submission_question: @submission_question } ) %>');
