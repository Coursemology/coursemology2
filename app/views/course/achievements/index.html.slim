= page_header do
  = new_button([current_course, :achievement]) if can?(:create, Course::Achievement)
table.table.achievement-list.table-hover
  thead
    tr
      th
      th = t('.description')
      th = t('.requirements')
      - if can?(:manage, Course::Achievement)
      th = t('.published')
      th
  tbody
    = render @achievements