import { defineMessages } from 'react-intl';

const translations = defineMessages({
  normal: {
    id: 'lib.translations.instance.users.roles.normal',
    defaultMessage: 'Normal',
  },
  instructor: {
    id: 'lib.translations.instance.users.roles.instructor',
    defaultMessage: 'Instructor',
  },
  administrator: {
    id: 'lib.translations.instance.users.roles.administrator',
    defaultMessage: 'Administrator',
  },
});

export default translations;
