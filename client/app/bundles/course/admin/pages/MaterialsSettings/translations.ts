import { defineMessages } from 'react-intl';

export default defineMessages({
  materialsSettings: {
    id: 'course.admin.materials.materialsSettings',
    defaultMessage: 'Materials settings',
  },
});
