export interface CourseUserData {
  id: number;
  name: string;
  imageUrl?: string;
  phantom?: boolean;
}

export interface CourseUserEntity {
  id: number;
  name: string;
  imageUrl?: string;
  phantom?: boolean;
}
